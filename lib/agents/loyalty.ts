// The punch-card Loyalty agent was removed — it was a giveaway model, not a
// revenue model. Its job (driving repeat visits) is now handled by the
// Flash Offer agent (goal-gap urgency) and the Win-Back / VIP agents, all of
// which keep the customer paying. This file is intentionally empty.
export {}
